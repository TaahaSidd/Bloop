spring.application.name=bloop

//spring.config.import=optional:file:.env[.properties]

# Server port
server.port=8080

# Server Address
server.address=0.0.0.0

# Database Connection
# spring.datasource.url=jdbc:postgresql://localhost:5432/SpicaLabs
# spring.datasource.username=taaha
# spring.datasource.password=SpicaLabs
# spring.datasource.driver-class-name=org.postgresql.Driver

# # Structural Config
# # Instruct JPA/Hibernate never to modify the database schema.
# spring.jpa.hibernate.ddl-auto=update
# spring.sql.init.mode=always

# Logs
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

